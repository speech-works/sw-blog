@speech-works:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}

# Ensure sharp's Linux native binary is installed when building on Netlify (Linux x64).
# Without this, only the macOS binary from local dev is present and image uploads fail.
sharp_ignore_global_libvips=true

# Sanity project the blog reads from (Content API). Find these in sanity.io/manage.
NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=production
# API version pin (date). Bump intentionally when you adopt new API features.
NEXT_PUBLIC_SANITY_API_VERSION=2024-10-01

# Absolute site origin, used for canonical URLs, OG tags, and the sitemap.
NEXT_PUBLIC_SITE_URL=https://speechworks.app

# Set to /blog when mounted under speechworks.app/blog via Cloudflare routing.
# Leave empty for the blog.speechworks.app subdomain fallback.
NEXT_PUBLIC_BASE_PATH=/blog

# Shared secret the Sanity publish webhook must present to /api/revalidate.
SANITY_REVALIDATE_SECRET=
